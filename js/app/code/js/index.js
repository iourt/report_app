angular.module('Huijm')
.controller('tIndex', function (
    $scope,
    $rootScope,
    $stateParams,

    widget,
    toJump
){

    // if (!$rootScope.UserInfo || !$rootScope.UserInfo.Auth) {
    //     toJump({
    //         direction: 'forward',
    //         router: 'report.login'
    //     });
    // }
});
